spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:file:~/escape
    driver-class-name: org.h2.Driver


telegram:
  bot:
    name:
    token:

observer:
  admins:

master:
  id:
  pw:
  name:

observing:
  page:
  noticeRoomNo:

app:
  telegramBot:
    name:
    token:

  admin:
    ids:

  master:
    id:
    pw:
    name:
