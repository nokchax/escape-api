spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true

app:
  telegramBot:
    name: peng
    token: giantPeng

  admin:
    ids: n,o,k,c,h,a,x

  master:
    id: nokchax
    pw: 123
    name: 광
