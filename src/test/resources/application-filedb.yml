spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
  datasource:
    data: /users.sql, /problems.sql


app:
  telegramBot:
    name: peng
    token: giantPeng

  admin:
    ids: n,o,k,c,h,a,x

  master:
    id: nokchax
    pw: 123
    name: 광
