spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
  datasource:
    data: /users.sql, /problems.sql #이 파일들내에 존재하는 쿼리를 실행시킨후 테스트를 진행함


app:
  telegramBot:
    name: peng
    token: giantPeng

  admin:
    ids: n,o,k,c,h,a,x

  master:
    id: nokchax
    pw: 123
    name: 광
